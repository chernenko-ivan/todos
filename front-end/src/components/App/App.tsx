import * as React from 'react';
import {FC} from "react";
  import {ListsContainer} from "../lists/ListsContainer";

export const App: FC = () => {
    return (
        <div>
            <ListsContainer/>
        </div>
    )
};
